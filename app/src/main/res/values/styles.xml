<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <!-- the style for the application -->
    <style name="AppBaseTheme" parent="@android:style/Theme.Holo.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>
    
    <!-- Application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Holo.Light">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/MyActionBar</item>
        <item name="android:actionMenuTextColor">@color/red</item>
    </style>
    
    <!-- the style for the actionbar -->
    <style name="MyActionBar" parent="@android:style/Widget.Holo.ActionBar">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:titleTextStyle">@style/MyActionBar_text</item>
        <item name="android:background">@color/red</item>
    </style>  
    
    <!-- the style for the actionbar text -->
    <style name="MyActionBar_text" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:textColor">@color/white</item>
    </style>      

</resources>
